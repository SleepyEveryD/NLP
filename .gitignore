# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Experiment outputs (logs are reproducible; keep them out of git by default)
experiments/runs/*
!experiments/runs/.gitkeep

# Data (corpus, dev sets, model caches)
# NOTE: the hand-checked offline dev set is force-tracked below (the notebook needs it on a fresh clone).
data/*
!data/.gitkeep
!data/dev_questions.jsonl
*.faiss
*.index

# Model / HF caches
.cache/
hf_cache/

# OS / editor
.DS_Store
*.swp
